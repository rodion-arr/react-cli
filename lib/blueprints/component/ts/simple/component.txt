__STYLES_IMPORT__

interface Props {
  className?: string;
}

export const __COMPONENT_NAME__ = ({
  className = '',
}: Props): JSX.Element => {
  return (
    <div className={`${className}`}>
    </div>
  );
};
